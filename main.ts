input.onButtonPressed(Button.A, function () {
    let list: number[] = []
    for (let waarde of list) {
        music.playTone(waarde, music.beat(BeatFraction.Whole))
    }
})
let lijst = [250, 500, 750, 1000, 1250, 1500]
basic.forever(function () {
	
})
